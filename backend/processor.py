# placeholder for future audio-processing logic
# currently, everything lives in audio_processor.py
from .audio_processor import AudioProcessor


class Processor(AudioProcessor):
    """Legacy alias; kept for API compatibility."""
    pass
